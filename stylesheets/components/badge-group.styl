// -------------
// Группа значков
// -------------
//
// Склеивает несколько значков в одну группу.
//
// ```
// .badge-group
//   .badge + .badge
// ```

.badge-group {
  display: inline-flex;
  vertical-align: middle;

  .badge {
    margin-left: -1px;
    border-radius: 0;

    &:first-child {
      border-top-left-radius: badge-radius;
      border-bottom-left-radius: badge-radius;
    }
    &:last-child {
      border-top-right-radius: badge-radius;
      border-bottom-right-radius: badge-radius;
    }
  }
}
