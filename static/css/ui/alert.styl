alert(bg, color)
  background: lighten(bg, 50%)
  color: color
  border: 4px solid bg

.alert
  clearfix()
  padding: 0 gutter
  border-radius: radius
  border: 1px solid #ccc

  for b in brand-colors
    &.{b}
      alert(lookup('brand-' + b), lookup('brand-' + b + '-text'))
